[project]
name = "cryptosanta"
version = "0.1.0"
description = "Cryptographic Secret Santa using ElGamal encryption"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "pydantic>=2.0",
    "modal>=0.68.0",
]
